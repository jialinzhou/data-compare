package com.compare.utils;


import com.compare.config.EncryptFieldConfig;

public class EncryptUtils {

    /**
     * 获取加密后的值
     * @param oriValue
     * @param encryptFieldConfig
     * @return
     */
    public static String getEncryptValue(Object oriValue, EncryptFieldConfig encryptFieldConfig){
        //todo 根据自己需要扩展
        return "";
    }
}
